# 포트폴리오

## 1. 프로필
- 이름: 김정현
- 이메일: richard.kim146@gmail.com
- GitHub: https://github.com/ranpia
- 소개: *"Java·Spring 기반 백엔드와 AI를 융합해 확장성과 안정성을 갖춘 서비스를 설계하는 개발자"*

---

## 2. 기술 스택
- **Backend**: Java 17/21, Spring Boot 3.x, Spring Security, Spring WebFlux, JPA, OAuth2, JWT  
- **Database**: MySQL, Oracle, JPA 멀티DB, RDS (모델링·튜닝 경험 포함)  
- **Infra & DevOps**: AWS(EC2, RDS, S3), Docker, GitHub Actions(CI/CD), Nginx  
- **AI/ML**: PyTorch, TensorFlow, 임베딩 기반 검색(RAG), OpenAI API (GPT-4, GPT-4o)  
- **ETC**: Swagger(OpenAPI), REST API, Git 협업  

---

## 3. 지원 동기 & 포부

### 보안성과 확장성 중심의 경험
저는 여러 프로젝트에서 **보안성과 확장성**을 핵심 가치로 두고 개발해 왔습니다.  
MoleLaw 프로젝트에서는 **JWT와 OAuth2 기반 인증**을 구현하여 안전한 사용자 인증 환경을 구축했고,  
Molecom 프로젝트에서는 **세션 회전(Session Rotation)과 단일 세션 강제** 로직을 설계해 안정적인 보안 체계를 마련했습니다.  
또한 OOP 원칙에 따라 **DTO와 서비스 계층을 분리하고 재사용 가능한 코드 구조**를 설계하여,  
새로운 기능 추가와 유지보수가 용이한 **확장성 있는 아키텍처**를 구현했습니다.  

### 기술 역량과 협업 경험
Spring Boot와 JPA를 중심으로 **RDBMS 모델링·튜닝**,  
Docker와 GitHub Actions 기반의 **CI/CD 자동화**,  
글로벌 예외 처리기를 통한 **안정적인 장애 대응**을 경험했습니다.  
협업에서는 단순한 코드 공유를 넘어, **feature/ 단위 브랜치를 생성하여 dev 브랜치로 Pull Request를 올리고 검토 후 main에 반영**하는 전략을 실천했습니다.  
이를 통해 **코드 리뷰 문화 정착과 충돌 최소화**를 이끌어내며, 효율적인 팀 개발 환경을 조성했습니다.  

---


## 4. 프로젝트 경험

### (1) MoleLaw – AI 기반 법률 상담 서비스
- **개요**: 법령/판례 실시간 조회 + 임베딩 RAG 기반 답변 생성 백엔드  
- **스택**: Java 17, Spring Boot 3.5.0, Spring Data JPA, MySQL, OpenAI GPT-4 / text-embedding-3-small, Spring WebFlux(WebClient), JJWT 0.11.5

- **보안/인증**
  - **JWT + OAuth2 인증**: 액세스(15분) / 리프레시(7~30일) 구조, 쿠키 기반 토큰 처리  
  - `CustomOAuth2UserService` + `OAuth2SuccessHandler`로 Google·Kakao 소셜 로그인 처리  
  - 설계 차원에서 **세션 저장소를 두지 않고 JWT를 채택**함으로써 **무상태(Stateless) 아키텍처** 지향 → 수평 확장성 확보  
  - **AES 메시지 암호화**: `EncryptUtil`로 모든 채팅 내용을 DB 저장 전 암호화, 조회 시 복호화

- **확장성/성능**
  - **온디맨드 벡터화 RAG**: 유사도 threshold(0.5) 미달 시 OpenLaw API에서 법령 본문 수집 → `LawEmbeddingService`로 재임베딩 → 재탐색  
  - **도메인 모델링**: `Law` → `LawChunk` → `LawEmbedding` 3계층 분리 → 검색·캐시 최적화  
  - **WebFlux WebClient** 기반 비동기 외부 API 호출 (GPT-4, OpenLaw API, 임베딩 API)  
  - **Docker + GitHub Actions** CI/CD 파이프라인 (Blue/Green 컨테이너 교체)

- **운영/품질**
  - **글로벌 예외 처리기**: `MolelawException` + `ErrorCode` enum (HTTP 400/401/403/404/500/502) + `traceId` 포함 응답  
  - OpenAPI(Swagger) 기반 계약 문서화  
  - 안티 프롬프트 인젝션 시스템 프롬프트 적용 (GPT-4 호출 시 법률 전문가 역할 고정)


#### 트러블슈팅 경험

1. **CORS 및 쿠키 인증 문제**
   - **문제**: 프론트엔드와 연동 시 JWT 토큰이 브라우저 쿠키에서 차단되어 인증 실패가 발생.  
   - **원인**: Spring Security 기본 설정에서 `SameSite=Lax` 쿠키 정책 적용 → 크로스도메인 요청 시 전송 차단.  
   - **해결**:  
     - `SameSite=None; Secure` 설정으로 크로스도메인 쿠키 허용.  
     - `SecurityConfig`의 `CorsConfigurationSource`를 커스터마이징하여 허용 origin과 header 명시.  
   - **성과**: OAuth2 로그인 흐름과 JWT 인증이 안정적으로 동작, 프론트엔드 개발자와의 연동 문제 해소.  

2. **유사도 검색 정확도 저하**
   - **문제**: 초기 벡터 검색에서 threshold(0.5) 기준 이하의 결과가 자주 발생 → GPT 응답 품질 저하.  
   - **원인**: 특정 법령의 임베딩 벡터가 충분히 생성되지 않아 corpus 부족.  
   - **해결**:  
     - `LawSearchService`에서 관련 법령 본문을 즉시 수집 후 `LawEmbeddingService`로 임베딩 생성.  
     - fallback 전략으로 `LawSimilarityService.cosineSimilarity()` 재검색 수행.  
   - **성과**: 검색 실패율을 크게 줄였고, **답변 정확도와 신뢰성 개선**.  

3. **예외 처리 일관성 부족**
   - **문제**: API 호출 시 발생하는 예외가 컨트롤러마다 다르게 처리되어, 클라이언트에서 예외 메시지 파싱이 어려움.  
   - **원인**: 개별 컨트롤러에서 예외 핸들링을 직접 처리.  
   - **해결**:  
     - `@RestControllerAdvice` 기반 글로벌 예외 처리기 도입.  
     - 모든 예외를 `MolelawException` → `ErrorResponse` 포맷으로 변환 (`ErrorCode` enum 중앙 관리).  
   - **성과**: 클라이언트 측 응답 표준화, 디버깅 속도와 유지보수 효율성 향상.  

4. **배포 시 무중단 환경 불안정**
   - **문제**: 초기 배포 과정에서 서버 다운타임 발생.  
   - **원인**: EC2에 직접 배포하는 방식으로 서비스 재기동 시점에 공백 발생.  
   - **해결**:  
     - Docker 기반으로 컨테이너 이미지를 빌드 후, GitHub Actions 파이프라인에서 자동 배포.  
     - **Blue/Green** 방식으로 컨테이너를 교체하여 다운타임 최소화.  
   - **성과**: **무중단 배포 환경 확보**, 운영 안정성 개선.  

---

### (2) Molecom – 의료 영상 관리 플랫폼
- **개요**: DICOM 메타데이터/이미지 스트리밍, 규제 대응 로그 관리  
- **보안/인증**
  - Spring Security 기반 엔드포인트 보호, 사용자 행위 로그 남김(민감 자원 접근 추적)  
  - **세션 회전(Session Rotation)**: `JwtAuthFilter` / `JwtSessionGuardFilter` + `SessionRotationService` 적용  
    → 단일 세션 강제 및 사일런트 리프레시로 안정적인 사용자 인증 환경 보장  
  - **도메인 예외 → 표준 에러 포맷**: `MolecomsException` + 전역 핸들러로 안정적 실패 응답  
- **확장성/성능**
  - **멀티 DB 분리**: Oracle(읽기·DICOM 메타) / MySQL(사용자·로그) 분담, **Tx 분리(oracleTx / mysqlTx)** 로 안정성 확보  
  - **WebFlux 기반 비동기 스트리밍**: `ResponseEntity<StreamingResponseBody>`로 대용량 이미지 **지연 최소화**  
  - **원격 SMB 스트리밍**: JCIFS-NG 기반 `CifsFileService`로 파일 IO 병목 완화  
- **운영/품질**
  - Swagger 문서화, `@Profile` 분리로 운영/개발 환경 격리  
- **역할**
  - 멀티 DB 아키텍처 설계, 스트리밍 API, 전역 예외·로그 정책 설계  

#### 트러블슈팅 경험  

1. **멀티 DB 환경 구축**  
   - **문제**: DICOM 메타데이터 조회(읽기 위주)와 사용자/접근 로그 적재(쓰기 위주)가 같은 DB에 존재해 경합 발생.  
   - **원인**: 회사에서 제공하는 **읽기 전용 Oracle DB**를 반드시 사용해야 했고, 동시에 사용자/로그 데이터는 별도 관리가 필요했음.  
   - **해결**:  
     - Oracle → 읽기 전용 DICOM 메타데이터 전담  
     - MySQL → 사용자/로그 저장 전담  
     - 트랜잭션을 `oracleTx`, `mysqlTx`로 분리해 독립적으로 관리.  
   - **성과**: 쿼리 경합 감소, 응답 지연 안정화, 한 DB 이슈가 타 DB로 전파되지 않아 **장애 범위 축소**.  

2. **JWT 기반 단일 세션 환경 구축 (확장 고려)**  
   - **문제**: 계정 공유 및 토큰 도난 방지 필요, 동시에 사용자 인증 상태를 일관되게 유지해야 함.  
   - **해결**:  
     - JWT(Access/Refresh) + **세션 회전(Session Rotation)** 전략 적용.  
     - 로그인 시 기존 세션 무효화 → 신규 토큰 발급(사일런트 회전)으로 단일 세션 강제.  
     - 다중 서버 확장 시 Redis 기반 세션 서버 + 토큰 블랙리스트로 동기화까지 고려.  
   - **성과**: 계정 도용 리스크 감소, 로그아웃/재로그인 흐름 안정화, **수평 확장 시 인증 일관성 확보**.  

3. **유저 '하드 삭제' → '논리 삭제' 전환**  
   - **문제**: 하드 삭제 시 기존 로그의 `user_id` 참조 단절 → 규제 감사/추적 불가.  
   - **해결**:  
     - 사용자 엔티티에 `status=INACTIVE`(또는 `deleted=true, deleted_at`) 필드 도입.  
     - 조회 시 활성 사용자만 노출, 로그 테이블은 기존 참조 유지.  
   - **성과**: 감사 추적성 보존(규제 대응), 데이터 무결성 유지, **복구·분석 용이성 향상**.  


---

### (3) ThinkTrip – AI 기반 여행 플래너
- **개요**: Spring Boot + Next.js 기반 맞춤 일정 추천/공유 서비스  
- **스택**: Java 17, Spring Boot 3.4.4, MySQL 8 (dev: local / prod: AWS RDS), Kakao + Google OAuth2, JWT (HMAC-SHA256, 24시간), Docker, GitHub Actions
- **보안/인증**
  - **Kakao + Google OAuth2 소셜 로그인** 및 JWT 인증 적용  
  - `application-dev.yml` / `application-prod.yml` 분리, 리디렉션 URL과 클라이언트 키를 환경변수화  
- **확장성/성능**
  - **RDBMS 모델링**: 여행 계획에 `startDate` / `endDate` 추가, 기존 `date`는 `lastModifiedDate`로 의미 분리  
  - **D-Day 계산**: `getDdayForUser()`가 여러 계획 중 **가장 이른 startDate** 기준으로 "D-5", "D-Day" 형태 반환  
  - **GPT 호출 제한**: 무료 5회/일 → 프리미엄 무제한 (`useGptOrThrow()` / `resetGptCallIfNewDay()`)  
  - **CI/CD**: GitHub Actions → Docker 이미지 빌드/배포, dev/main 브랜치 전략으로 안정 릴리스  
- **역할**
  - 백엔드 인증, 데이터 모델링, 배포 자동화 및 운영 가이드 문서화  

#### 트러블슈팅 경험  

1. **소셜 로그인 전환 및 환경변수 정리**  
   - **문제**: 기존 Google OAuth2 적용 상태에서 Kakao OAuth2 추가 필요.  
   - **해결**:  
     - OAuth2 Client 설정을 Kakao + Google 멀티 provider로 수정.  
     - `application-dev.yml` / `application-prod.yml` 분리, 리디렉션 URL과 클라이언트 키를 환경변수화.  
   - **성과**: 운영 환경에서도 안전하게 배포 가능, 프론트엔드와의 협업 시 설정 오류 방지.  

2. **여행 계획 데이터 모델링 보완**  
   - **문제**: 기존 `date` 필드만으로는 일정 기간 관리 및 D-Day 계산이 어려움.  
   - **해결**:  
     - `startDate`, `endDate` 추가; 기존 `date`는 `lastModifiedDate`로 의미 변경.  
     - D-Day 계산 로직에서 가장 빠른 `startDate`를 기준으로 전역 D-Day 노출.  
   - **성과**: 일정 관리 정확성 강화, D-Day 기반 알림/UX 기능 확장 가능.  

3. **API 호출 제한 및 비용 제어**  
   - **문제**: GPT API 호출 비용 관리 필요.  
   - **해결**:  
     - 무료 계정은 하루 5회 제한, 프리미엄 계정은 무제한 호출 가능하도록 설계.  
     - 호출 횟수 기록용 컬럼(`gptCallCount`, `lastGptCallDate`) 및 제한 로직 구현.  
   - **성과**: 비용 최적화, 서비스 지속 가능성 확보.  


---

### (4) Fruit Box AI – 게임 자동화 및 강화학습
- **개요**: OCR + RL로 실시간 보드 인식/의사결정 자동화  
- **스택**: Python 3.x, PyTorch, OpenCV(cv2), Selenium WebDriver, NumPy, Pandas

- **파이프라인 구조**
  - 스크린샷 캡처 → OpenCV **HSV 색상 필터**로 사과 탐지(contour 분석, 8~30px 반경) → 10×17 그리드 구성  
  - CNN OCR(`OCR_CNN`: 2-layer Conv, 32×32 입력, 9-class 분류)으로 숫자 인식  
  - `ConvDQN`(3-layer Conv) 에이전트가 합이 10인 그룹 선택 → Selenium 드래그 실행

- **강화학습 설계**
  - Experience Replay (capacity 10,000), ε-greedy (1.0 → 0.1, decay 0.995)  
  - 보상: 유효 그룹 → 제거된 사과 수 / 무효 그룹 → -1 (종료)  
  - 1,000 에피소드 학습, **160점 달성 시 조기 종료**

- **품질/검증**
  - CNN OCR Softmax 신뢰도 필터링 → 낮은 신뢰도 시 pytesseract 보조 인식으로 오검 방지  
  - 실제 플레이 데이터(보드 상태 + 선택 결과) 자동 수집 → 지속 학습 구조  

#### 트러블슈팅 경험  

1. **딥러닝 OCR의 일반화 성능 저하**  
   - **문제**: 학습 데이터에서는 정확도가 높았으나 실제 게임 환경에서는 오인식 발생.  
   - **해결**:  
     - Softmax 신뢰도 기반 필터링 추가.  
     - 낮은 신뢰도의 숫자는 pytesseract 보조 인식 또는 룰 기반 후처리.  
   - **성과**: 인식 오류 감소, 게임 자동화 안정성 개선.  

2. **실시간 처리 속도 병목**  
   - **문제**: 스크린샷 처리/OCR 속도로 인해 2분 제한 시간 내 점수를 극대화하지 못함.  
   - **해결**:  
     - OpenCV 기반 전처리 최적화 (HSV 마스크, 형태학 연산 효율화).  
     - OCR 결과 캐시 및 중복 연산 제거.  
   - **성과**: 프레임 처리 속도 개선, **최고 170점 달성**.  

3. **강화학습 정책 안정화**  
   - **문제**: 단순 그리디 탐색 기반에서는 장기 점수가 낮게 수렴.  
   - **해결**:  
     - 실제 플레이 데이터(보드 상태 + 선택 결과) 자동 수집.  
     - `ConvDQN` 정책 네트워크 학습 후 게임 내 반복 적용.  
   - **성과**: 반복 플레이를 통한 성능 향상, **장기 점수 160점 이상 안정적 달성**.  


---

### (5) AI_Diary (MindNote) – 감정 분석 일기 앱
- **개요**: 일기 기반 감정 분석 + AI 상담 + 콘텐츠 추천 서비스  
- **스택**: Java 21, Spring Boot 3.5.0, Spring Security, JPA, OAuth2 (Google + Kakao), JWT / React 19.1.0 + Vite / OpenAI GPT-4o
- **주요 기능**
  - GPT-4o 기반 감정 분석: 일기 텍스트 → 감정 상태 추론 → 감정 강도 비율 계산 → 일별 요약  
  - 주간/월간 감정 트렌드 그래프, 감정 기반 콘텐츠(음악·행동) 추천  
  - 프리미엄: PDF 리포트 생성, AI 맞춤 피드백, 전문 상담 연결  
  - Google + Kakao OAuth2 소셜 로그인 + JWT 인증  
- [🔗 GitHub Repo](https://github.com/ranpia/AI_Diary)

---

## 5. 협업 & 리더십 경험
- 팀 프로젝트에서 **Spring 백엔드 총괄** 및 아키텍처 설계 주도  
- GitHub 브랜치 전략(feature/ → dev → main) + GitHub Actions CI/CD 파이프라인 구축  
- ERD, 시퀀스 다이어그램, Swagger API 문서화로 **체계적인 협업 환경 조성**
